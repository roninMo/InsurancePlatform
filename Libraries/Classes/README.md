# Classes

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build Classes` to build the library.

## Running unit tests

Run `nx test Classes` to execute the unit tests via [Vitest](https://vitest.dev/).
